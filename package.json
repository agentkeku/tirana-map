{
  "name": "tirana-insider",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install && npx prisma generate",
    "start": "cd backend && npx prisma db push && npx prisma db seed && node src/index.js"
  }
}
